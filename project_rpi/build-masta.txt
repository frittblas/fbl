put all *.c-files in the same directory (fbl, fbl_test, dllink and a_star).

gcc -std=c99 *.c -o fbl_test `sdl2-config --cflags --libs` -lSDL2_image -lSDL2_ttf -lSDL2_mixer -lchipmunk -llua5.2 -lm

something like that... try -02 also

